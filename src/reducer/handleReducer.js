import React from "react";

function handleReducer(state, action) {
  switch (action.type) {
    case "SUBMIT":
      return {};

      break;

    default:
      break;
  }
}

export default handleReducer;
